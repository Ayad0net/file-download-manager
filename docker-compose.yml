services:
  download-manager:
    build: .
    container_name: download-manager
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PORT=8080
      - DOWNLOAD_PATH=./downloads
